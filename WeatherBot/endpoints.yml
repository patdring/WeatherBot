# Creates the connection to Rasa SDK Action Server
# Contains webhook configuration for the custom actions
# Start Action Server: rasa run actions
action_endpoint:
  url: http://localhost:5055/webhook

